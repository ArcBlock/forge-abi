defmodule ForgeWeb.GQL.Forge.Resolver do
  @moduledoc false
  def add_source_for_dataloader(context) do
    context
  end

  def multisig(_parent, _args, _info) do
    {:ok, %{}}
  end

  def get_tx(_parent, _args, _info) do
    {:ok, %{}}
  end

  def get_block(_parent, _args, _info) do
    {:ok, %{}}
  end

  def get_blocks(_parent, _args, _info) do
    {:ok, %{}}
  end

  def get_unconfirmed_txs(_parent, _args, _info) do
    {:ok, %{}}
  end

  def get_chain_info(_parent, _args, _info) do
    {:ok, %{}}
  end

  def search(_parent, _args, _info) do
    {:ok, %{}}
  end

  def get_net_info(_parent, _args, _info) do
    {:ok, %{}}
  end

  def get_validators_info(_parent, _args, _info) do
    {:ok, %{}}
  end

  def get_config(_parent, _args, _info) do
    {:ok, %{}}
  end

  def load_file(_parent, _args, _info) do
    {:ok, %{}}
  end

  def pin_file(_parent, _args, _info) do
    {:ok, %{}}
  end

  def get_account_state(_parent, _args, _info) do
    {:ok, %{}}
  end

  def get_asset_state(_parent, _args, _info) do
    {:ok, %{}}
  end

  def get_stake_state(_parent, _args, _info) do
    {:ok, %{}}
  end

  def get_forge_state(_parent, _args, _info) do
    {:ok, %{}}
  end

  def load_wallet(_parent, _args, _info) do
    {:ok, %{}}
  end

  def list_wallet(_parent, _args, _info) do
    {:ok, %{}}
  end

  def mutation_create_tx(_parent, _args, _info) do
    {:ok, %{}}
  end

  def mutation_send_tx(_parent, _args, _info) do
    {:ok, %{}}
  end

  def mutation_unsubscribe(_parent, _args, _info) do
    {:ok, %{}}
  end

  def mutation_store_file(_parent, _args, _info) do
    {:ok, %{}}
  end

  def mutation_create_wallet(_parent, _args, _info) do
    {:ok, %{}}
  end

  def mutation_recover_wallet(_parent, _args, _info) do
    {:ok, %{}}
  end

  def mutation_remove_wallet(_parent, _args, _info) do
    {:ok, %{}}
  end

  def mutation_declare_node(_parent, _args, _info) do
    {:ok, %{}}
  end

  def mutation_declare(_parent, _args, _info) do
    {:ok, %{}}
  end

  def subscription_subscribe(_parent, _args, _info) do
    {:ok, %{}}
  end

  def subscribe_trigger(_parent, _args, _info) do
    {:ok, %{}}
  end
end
